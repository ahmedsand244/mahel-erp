name: mahel_pos_mobile
description: "النماء ERP & POS Mobile App - Offline-First POS & Cloud Sync"
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.1.0
  sqflite: ^2.3.0
  path: ^1.9.0
  path_provider: ^2.1.1
  connectivity_plus: ^5.0.2
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
